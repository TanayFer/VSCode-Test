CREATE TABLE ACCOUNT (
    AccountNo INT PRIMARY KEY,
    AccountType VARCHAR(50),
    Balance DECIMAL(15, 2)
);

ALTER TABLE ACCOUNT ADD Status VARCHAR(20) DEFAULT 'Active';

ALTER TABLE ACCOUNT ADD CONSTRAINT CHK_Balance CHECK (Balance >= 0);

ALTER TABLE ACCOUNT RENAME COLUMN AccountType TO TypeOfAccount;

ALTER TABLE ACCOUNT DROP CONSTRAINT CHK_Balance;

DROP TABLE ACCOUNT;