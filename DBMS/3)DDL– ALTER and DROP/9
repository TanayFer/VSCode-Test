CREATE TABLE PROJECT (
    ProjectID INT PRIMARY KEY,
    ProjectName VARCHAR(50),
    Budget DECIMAL(12, 2)
);

ALTER TABLE PROJECT ADD CONSTRAINT CHK_Budget CHECK (Budget > 0);

ALTER TABLE PROJECT MODIFY ProjectName VARCHAR(150);

ALTER TABLE PROJECT ADD Deadline DATE;

ALTER TABLE PROJECT DROP COLUMN Deadline;

DROP TABLE PROJECT;